@using System.Web.Mvc;
@{
    // Execute code common for ALL the cshtml here
    // We've too many partial view so better to set master page in those few pages where we need it 
    // Layout = "~/Views/Shared/_SiteMasterLayout.cshtml";
}